# Get a free key at https://openrouter.ai/keys
OPENROUTER_API_KEY=

# Optional — leave unset and the server will auto-pick a strong free model
OPENROUTER_MODEL=

# Railway sets PORT automatically; this is only used for local dev
PORT=3000
